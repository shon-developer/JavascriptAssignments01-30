// ? Create a countries.js file and store the countries name into this file, create a file web_tech.js and store the
// ? popular web technology name into this file. Access both file in a third file named main.js

import { countries } from "./countries";
console.log(countries);

import { popularTechnologies } from "./web_techs";
console.log(popularTechnologies);
